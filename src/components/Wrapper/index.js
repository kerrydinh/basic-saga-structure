import styled from 'styled-components';

const Wrapper = styled.div`
    padding-left: 250px;
    display: block;
    height: 1000px;
    position: absolute;
    float: left;
    z-index: -1;
`;

export default Wrapper;